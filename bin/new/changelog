#!/usr/bin/env bash
set -euo pipefail

# Usage:
#   ./bin/new/changelog
#   ./bin/new/changelog "2026-03-17T14:14:56Z"

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(cd "$script_dir/../.." && pwd)"

exec "$repo_root/scripts/new-changelog.sh" "$@"
