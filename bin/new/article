#!/usr/bin/env bash
set -euo pipefail

# Usage:
#   ./bin/new/article "Article Title"

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(cd "$script_dir/../.." && pwd)"

exec "$repo_root/scripts/new-article.sh" "$@"
