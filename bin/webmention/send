#!/usr/bin/env bash
set -euo pipefail

# Usage:
#   ./bin/webmention/send <source-url> <target-url>

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(cd "$script_dir/../.." && pwd)"

exec node "$repo_root/scripts/webmention-send.mjs" "$@"
